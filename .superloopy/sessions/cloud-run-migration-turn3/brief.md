Cloud Run migration Turn 3: migrate tuition reads and writes, payment append/update/delete, tuition notes, contact logs, status updates, and amount overrides to authenticated Cloud Run APIs backed by Firestore transactions; preserve safe legacy fallback, prevent duplicate or ambiguous writes, deploy, and verify read-only plus isolated transactional tests through Chrome academy profile.
